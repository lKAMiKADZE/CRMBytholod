@{
    ViewData["Title"] = "Главная";
}

    <div class="text-center">
        <h1 class="display-4">Добро пожаловать</h1>
        <h3>CRM Быт холод 1</h3>
        <h4>V2222</h4>
        <br>
        @if (User.Identity.Name != null)
        {
            <p style="font-size: x-large;">Перейти в <a asp-action="Orders">заявки</a>.</p>
        }
    </div>
